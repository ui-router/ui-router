# Generated by tools/render-ci-workflow.mjs from migration/ci-gates.json.
# Do not edit this file directly.
name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  CI_RUNTIME_IMAGE_DIGEST: sha256:c091b21d9fae78c76e85cd4356431e9b018402f172a214fc7d7a5e9a7e29d8ac
  CI: "1"
  HUSKY: "0"
  LC_ALL: "C"
  TZ: "UTC"
  UV_INDEX_URL: "https://pypi.org/simple"
  UV_CACHE_DIR: "/tmp/uirouter-ci-uv-cache"
  PLAYWRIGHT_BROWSERS_PATH: "/ms-playwright"
  CYPRESS_CACHE_FOLDER: "/root/.cache/Cypress"
  CHROME_BIN: "/ms-playwright/chromium-1234/chrome-linux64/chrome"

jobs:
  contracts:
    name: Contracts, install, and policy
    runs-on: ubuntu-24.04
    container:
      image: mcr.microsoft.com/playwright@sha256:c091b21d9fae78c76e85cd4356431e9b018402f172a214fc7d7a5e9a7e29d8ac
    timeout-minutes: 90
    steps:
      - name: Check out exact revision
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
      - name: Trust checked-out worktree
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - name: Install pinned Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 24.19.0
          check-latest: false
      - name: Install pinned npm
        run: npm install --global npm@11.17.0 --ignore-scripts --no-audit --no-fund
      - name: Bootstrap pinned registry tarballs
        run: node tools/bootstrap-ci-registry-tarballs.mjs
      - name: Bootstrap pinned uv
        run: node tools/bootstrap-ci-uv.mjs
      - name: Run contracts gate
        run: node tools/run-ci-gate.mjs --job contracts
      - name: Upload compact contracts failure evidence
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: ci-failure-contracts-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            .ci-results/contracts/**
          if-no-files-found: ignore
          include-hidden-files: true
          retention-days: 7
  source:
    name: Source-linked quality and tests
    runs-on: ubuntu-24.04
    container:
      image: mcr.microsoft.com/playwright@sha256:c091b21d9fae78c76e85cd4356431e9b018402f172a214fc7d7a5e9a7e29d8ac
    timeout-minutes: 90
    steps:
      - name: Check out exact revision
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
      - name: Trust checked-out worktree
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - name: Install pinned Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 24.19.0
          check-latest: false
      - name: Install pinned npm
        run: npm install --global npm@11.17.0 --ignore-scripts --no-audit --no-fund
      - name: Bootstrap pinned registry tarballs
        run: node tools/bootstrap-ci-registry-tarballs.mjs
      - name: Run source gate
        run: node tools/run-ci-gate.mjs --job source
      - name: Upload compact source failure evidence
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: ci-failure-source-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            .ci-results/source/**
          if-no-files-found: ignore
          include-hidden-files: true
          retention-days: 7
  packages:
    name: Production packages and consumers
    runs-on: ubuntu-24.04
    container:
      image: mcr.microsoft.com/playwright@sha256:c091b21d9fae78c76e85cd4356431e9b018402f172a214fc7d7a5e9a7e29d8ac
    timeout-minutes: 90
    steps:
      - name: Check out exact revision
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
      - name: Trust checked-out worktree
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - name: Install pinned Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 24.19.0
          check-latest: false
      - name: Install pinned npm
        run: npm install --global npm@11.17.0 --ignore-scripts --no-audit --no-fund
      - name: Bootstrap pinned registry tarballs
        run: node tools/bootstrap-ci-registry-tarballs.mjs
      - name: Run packages gate
        run: node tools/run-ci-gate.mjs --job packages
      - name: Upload content-addressed package artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: packages-${{ github.sha }}
          path: .ci-artifacts/packages/
          if-no-files-found: error
          compression-level: 0
          include-hidden-files: true
          retention-days: 7
      - name: Upload compact packages failure evidence
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: ci-failure-packages-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            .ci-results/packages/**
            .migration-work/package-proof/**
            .ci-artifacts/packages/hashes.json
          if-no-files-found: ignore
          include-hidden-files: true
          retention-days: 7
  browser:
    name: Workspace browser E2E
    runs-on: ubuntu-24.04
    container:
      image: mcr.microsoft.com/playwright@sha256:c091b21d9fae78c76e85cd4356431e9b018402f172a214fc7d7a5e9a7e29d8ac
    timeout-minutes: 90
    steps:
      - name: Check out exact revision
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
      - name: Trust checked-out worktree
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - name: Install pinned Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 24.19.0
          check-latest: false
      - name: Install pinned npm
        run: npm install --global npm@11.17.0 --ignore-scripts --no-audit --no-fund
      - name: Bootstrap pinned registry tarballs
        run: node tools/bootstrap-ci-registry-tarballs.mjs
      - name: Run browser gate
        run: node tools/run-ci-gate.mjs --job browser
      - name: Upload compact browser failure evidence
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: ci-failure-browser-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            .ci-results/browser/**
            **/test-results/**
            **/playwright-report/**
            **/cypress/screenshots/**
            **/cypress/videos/**
          if-no-files-found: ignore
          include-hidden-files: true
          retention-days: 7
  docs:
    name: Documentation waiver gate
    runs-on: ubuntu-24.04
    container:
      image: mcr.microsoft.com/playwright@sha256:c091b21d9fae78c76e85cd4356431e9b018402f172a214fc7d7a5e9a7e29d8ac
    timeout-minutes: 10
    steps:
      - name: Check out exact revision
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
      - name: Trust checked-out worktree
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - name: Install pinned Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 24.19.0
          check-latest: false
      - name: Install pinned npm
        run: npm install --global npm@11.17.0 --ignore-scripts --no-audit --no-fund
      - name: Bootstrap pinned registry tarballs
        run: node tools/bootstrap-ci-registry-tarballs.mjs
      - name: Run docs gate
        run: node tools/run-ci-gate.mjs --job docs
      - name: Upload compact docs failure evidence
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: ci-failure-docs-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            .ci-results/docs/**
          if-no-files-found: ignore
          include-hidden-files: true
          retention-days: 7
  integration:
    name: Isolated integration (${{ matrix.shard }})
    needs:
      - packages
    runs-on: ubuntu-24.04
    container:
      image: mcr.microsoft.com/playwright@sha256:c091b21d9fae78c76e85cd4356431e9b018402f172a214fc7d7a5e9a7e29d8ac
    timeout-minutes: 120
    strategy:
      fail-fast: false
      matrix:
        shard:
          - core
          - angular
          - angularjs
          - react
          - react-hybrid
    steps:
      - name: Check out exact revision
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
      - name: Trust checked-out worktree
        run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - name: Install pinned Node
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 24.19.0
          check-latest: false
      - name: Install pinned npm
        run: npm install --global npm@11.17.0 --ignore-scripts --no-audit --no-fund
      - name: Bootstrap pinned registry tarballs
        run: node tools/bootstrap-ci-registry-tarballs.mjs
      - name: Download producer artifacts
        uses: actions/download-artifact@d3f86a106a0bac45b974a628896c90dbdf5c8093 # v4.3.0
        with:
          name: packages-${{ github.sha }}
          path: .ci-input/packages
      - name: Run isolated integration shard
        run: node tools/run-ci-gate.mjs --job integration --shard ${{ matrix.shard }}
      - name: Upload compact integration-${{ matrix.shard }} failure evidence
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: ci-failure-integration-${{ matrix.shard }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            .ci-results/integration-${{ matrix.shard }}/**
            .migration-work/i02/ci-${{ matrix.shard }}/**
          if-no-files-found: ignore
          include-hidden-files: true
          retention-days: 7
  required:
    name: Required
    if: always()
    needs:
      - contracts
      - source
      - packages
      - integration
      - browser
      - docs
    runs-on: ubuntu-24.04
    steps:
      - name: Require every CI gate
        shell: bash
        run: |
          set -euo pipefail
          test "${{ needs.contracts.result }}" = "success" || { echo "contracts: ${{ needs.contracts.result }}" >&2; exit 1; }
          test "${{ needs.source.result }}" = "success" || { echo "source: ${{ needs.source.result }}" >&2; exit 1; }
          test "${{ needs.packages.result }}" = "success" || { echo "packages: ${{ needs.packages.result }}" >&2; exit 1; }
          test "${{ needs.integration.result }}" = "success" || { echo "integration: ${{ needs.integration.result }}" >&2; exit 1; }
          test "${{ needs.browser.result }}" = "success" || { echo "browser: ${{ needs.browser.result }}" >&2; exit 1; }
          test "${{ needs.docs.result }}" = "success" || { echo "docs: ${{ needs.docs.result }}" >&2; exit 1; }
